# Source Instructions

- Keep modules objective-oriented and small.
- Add comments where the IBKR OAuth flow is non-obvious.
- Avoid logging secrets, signatures, decrypted prepends, or live session tokens.


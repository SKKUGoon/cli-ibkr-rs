# Model Instructions

- Add typed request/response models only when they clarify the public crate API.
- Preserve raw IBKR JSON in CLI output unless a command explicitly documents a transformed schema.

